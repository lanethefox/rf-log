{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window (events + window control).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
